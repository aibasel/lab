#! /bin/bash

# Test setup: see bitbucket-pipelines.yml

set -exuo pipefail

# Check imports and unused/missing variables.
python -m flake8 --exclude=build,data,external,conf.py,.tox,.hg --max-line-length=90
