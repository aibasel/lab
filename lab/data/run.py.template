#! /usr/bin/env python2
# -*- coding: utf-8 -*-

import os

from lab.calls import call
from lab.calls import log

run_log = open('run.log', 'w')
run_err = open('run.err', 'w')
redirects = {'stdout': run_log, 'stderr': run_err}

# Make sure we're in the run directory.
os.chdir(os.path.dirname(os.path.abspath(__file__)))

%(calls)s

if os.path.getsize(run_err.name) != 0:
    log.add_unexplained_error('output-to-' + run_err.name)

for f in [run_log, run_err]:
    f.close()
    log.delete_file_if_empty(f.name)
